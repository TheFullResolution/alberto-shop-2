{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "echo 'Server build complete'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}